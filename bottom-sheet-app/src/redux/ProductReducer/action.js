export const toggleBottomSheet = () => ({
    type: 'TOGGLE_BOTTOM_SHEET',
  });
  
  export const setCurrentSnapPoint = (snapPoint) => ({
    type: 'SET_CURRENT_SNAP_POINT',
    payload: snapPoint,
  });